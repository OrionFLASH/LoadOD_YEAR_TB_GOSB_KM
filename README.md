# LoadOD YEAR_TB_GOSB_KM

Скрипт для массовой обработки файлов ОД (`xlsx`) с выделением последнего КМ, расчетом динамики, добавлением кластера из `OrgUnit` и выгрузкой итоговых файлов в структуру `OUT/YYYY/DD-MM`.

Текущая рабочая версия: `Main.py` v3.0 (актуализировано по фактическим доработкам).

---

## 1. Что решает проект

- Загружает пачку Excel-файлов из `IN`.
- Удаляет строки с `КМ = "Серая зона"`.
- Нормализует ключи:
  - `ИНН_12` -> 12 символов;
  - `ТН 10` -> 8 символов.
- Определяет последний КМ по ключу `(ИНН_12, Код ТБ, Код ГОСБ)`.
- Формирует динамику по последнему КМ:
  - суммы прошлого/текущего ОД;
  - прирост;
  - темп прироста по правилам ToDo.
- Подтягивает `Кластер` из `IN/OrgUnit/*.csv`.
- Сохраняет результаты в `OUT/YYYY/DD-MM`.

---

## 2. Требования к окружению

- Python 3.10+ (подходит Anaconda).
- Библиотеки:
  - `pandas`
  - `openpyxl`
- Стандартная библиотека Python:
  - `os`, `glob`, `json`, `logging`, `datetime`, `re`, `concurrent.futures`, `warnings`.

---

## 3. Структура проекта

```text
LoadOD_YEAR_TB_GOSB_KM/
├── Main.py
├── Config.json
├── README.md
├── IN/
│   ├── *.xlsx
│   └── OrgUnit/
│       └── *.csv
├── OUT/
│   └── YYYY/
│       └── DD-MM/
│           ├── 01_raw_combined_<TIMESTAMP>.csv
│           ├── 02_aggregated_<TIMESTAMP>.csv
│           ├── 03_last_km_by_inn_tb_<TIMESTAMP>.csv/.xlsx
│           ├── 04_km_dynamics_<TIMESTAMP>.csv/.xlsx
│           ├── 05_final_result_with_cluster_<TIMESTAMP>.csv/.xlsx
│           └── 06_statistics_<TIMESTAMP>.xlsx
└── log/
    ├── INFO_parser_YYYYMMDD_HH.log
    └── DEBUG_parser_YYYYMMDD_HH.log
```

---

## 4. Конфигурация (`Config.json`)

Все параметры запуска вынесены в `Config.json`.

### 4.1. Параметры и назначение

- `paths.input_folder` — папка входных `xlsx`.
- `paths.output_folder` — корень выходных файлов.
- `paths.log_folder` — папка логов.
- `paths.orgunit_folder` — папка справочника OrgUnit.
- `patterns.input_file_glob` — маска входных Excel-файлов.
- `patterns.orgunit_file_glob` — маска CSV-файлов OrgUnit.
- `excel.input_sheet` — имя листа в `xlsx`.
- `processing.max_workers` — число потоков загрузки.
- `processing.xlsx_export_limit` — порог для сохранения `.xlsx`.
- `columns_to_load` — список читаемых колонок из `xlsx`.
- `group_key` — ключ группировки агрегата.
- `output.layout_by_date` — раскладка по дате (`true/false`).
- `output.date_layout` — формат подпапок, текущий: `YYYY/DD-MM`.
- `output.*_basename` — базовые имена выходных файлов.

### 4.2. Важно

- `Config.json` обязателен: без него скрипт завершится ошибкой.
- Все пути задаются относительно каталога проекта.

---

## 5. Входные данные

### 5.1. Основные файлы `IN/*.xlsx`

Обязательные колонки:
- `ИНН_12`
- `ТН 10`
- `Код ТБ`
- `Код ГОСБ`
- `ТБ`
- `ГОСБ`
- `КМ`
- `Дата загрузки`
- `ПРОШЛЫЙ ГОД ОД, тыс. руб.`
- `ТЕКУЩИЙ ГОД ОД, тыс. руб.`

Все временные файлы Excel (`~$...`) игнорируются.

### 5.2. Файл OrgUnit `IN/OrgUnit/*.csv`

Используются колонки:
- `GOSB_CODE`
- `CLUSTER`

Преобразование:
- `GOSB_CODE` -> `Код ГОСБ`
- `CLUSTER` -> `Кластер`

---

## 6. Логика обработки

1. Поиск `xlsx` по маске из `Config.json`.
2. Параллельная загрузка файлов (`ThreadPoolExecutor`).
3. Нормализация ключей и типов.
4. Удаление строк `КМ = "Серая зона"`.
5. Объединение в единый слой.
6. Агрегация по ключу `group_key`.
7. Выделение последнего КМ и КМ по датам.
8. Динамика по последнему КМ.
9. Обогащение динамики кластером из OrgUnit.
10. Сохранение выходных файлов и статистики.

---

## 7. Расчет метрик

### 7.1. Прирост

`Прирост ОД = Сумма ТЕКУЩИЙ ГОД ОД - Сумма ПРОШЛЫЙ ГОД ОД`

### 7.2. Темп прироста (%)

- Если прошлый = 0:
  - текущий > 0 -> 100
  - текущий < 0 -> -100
  - текущий = 0 -> 0
- Иначе:
  - `(текущий - прошлый) / abs(прошлый) * 100`

---

## 8. Выходные файлы

Выход складывается в:
- `OUT/YYYY/DD-MM`

Где:
- `YYYY` — год запуска (4 цифры)
- `DD-MM` — день-месяц с ведущими нулями

Создаются:
- `01_raw_combined_<TIMESTAMP>.csv`
- `02_aggregated_<TIMESTAMP>.csv`
- `03_last_km_by_inn_tb_<TIMESTAMP>.csv` и при малом объеме `.xlsx`
- `04_km_dynamics_<TIMESTAMP>.csv` и при малом объеме `.xlsx`
- `05_final_result_with_cluster_<TIMESTAMP>.csv` и при малом объеме `.xlsx`
- `06_statistics_<TIMESTAMP>.xlsx`

Правило экспорта:
- CSV — всегда;
- XLSX — только если строк меньше `processing.xlsx_export_limit`.

---

## 9. Логирование

Логи пишутся в `log`:
- `INFO_parser_YYYYMMDD_HH.log`
- `DEBUG_parser_YYYYMMDD_HH.log`

Формат DEBUG-строки:

`дата время - [уровень] - сообщение [class: <имя класса> | def: <имя функции>]`

---

## 10. Запуск

```bash
python Main.py
```

Перед запуском:
- проверить `Config.json`;
- положить входные `xlsx` в `IN`;
- положить OrgUnit CSV в `IN/OrgUnit`.

---

## 11. Список основных функций (`Main.py`)

- `load_config()` — загрузка и валидация конфигурации.
- `setup_logger()` — настройка INFO/DEBUG логов.
- `log_debug()` — единый DEBUG-лог с `class/def`.
- `normalize_inn()` — нормализация ИНН.
- `normalize_tn10()` — нормализация ТН 10.
- `normalize_code()` — нормализация кодов.
- `save_table()` — универсальное сохранение CSV/XLSX.
- `calc_growth_rate()` — расчет темпа прироста.
- `get_max_od_row()` — выбор строки с max текущим ОД.
- `load_single_file()` — загрузка и очистка одного `xlsx`.
- `load_orgunit_mapping()` — загрузка маппинга `Код ГОСБ -> Кластер`.
- `auto_fit_worksheet()` — ширина колонок статистики.
- `main()` — основной сценарий обработки.

---

## 12. История изменений

### v2.2
- Базовая логика загрузки, агрегации и динамики.

### v2.3
- Оптимизация этапов и правила экспорта по порогу строк.

### v3.0 (текущая)
- Введен обязательный `Config.json`.
- Параметры/пути/маски/имена файлов вынесены в конфиг.
- Добавлена интеграция OrgUnit и поле `Кластер`.
- Нормализация `ИНН_12` и `ТН 10` по фиксированной длине.
- Добавлено файловое логирование `INFO/DEBUG`.
- Выход перенесен в структуру `OUT/YYYY/DD-MM`.
- Сгенерирован и проверен большой тестовый набор (12 файлов) для нагрузочного прогона.
